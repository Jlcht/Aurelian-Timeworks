import React from 'react';
import './Hero.css';

const Hero = () => {
    return (
        <section className="hero">
            <h1>Build Fast. Deploy Smarter.</h1>
            <p>Your personal portfolio backend powered by React & Firebase.</p>
            <button className="hero-btn">Get Started</button>
        </section>
    );
};

export default Hero;
